# 隐私与数据边界

## 浏览器本地数据

造途匿名封测版将职业资料、JD、岗位记录、材料草稿和反馈保存在访问者当前浏览器的 `localStorage`。清理浏览器站点数据可能导致资料丢失，请按需使用产品内的 JSON 备份功能。

## 简历导入

- DOCX 与带文字层的 PDF 在浏览器本地解析，不上传到服务端；
- 最大 6MB，PDF 最多读取前 12 页；
- 扫描件 PDF、加密 PDF 和旧版 `.doc` 当前不支持；
- 导入结果只会成为待确认草稿，未确认内容不能进入正式材料。

## Word 生成

用户主动点击生成 Word 后，本次所需职业资料、JD，以及可选的 DOCX 模板或证件照，会通过 HTTPS 发送至匿名文档接口。接口在请求内存中生成并返回 DOCX，不建立账户、不写入业务数据库、不做对象存储或长期保存。

## 不应提交的内容

不要在仓库 Issue、讨论或匿名反馈中提交：

- 姓名、手机号、邮箱、身份证号或精确住址；
- 完整简历、完整 JD、证件照或 Word/PDF 原件；
- 客户名称、内部设备参数、未公开代码、日志或现场截图；
- 密码、Token、Cookie、私钥或其他认证信息。

涉及视觉问题时，请使用已打码截图，并确保无法识别个人、企业、学校或客户。
